<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>FitGrocer Admin</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap"
    rel="stylesheet" />
  <link rel="stylesheet" href="/CSS/viewNutritions.css" />
</head>

<body>
  <nav class="navbar">
    <div class="navbar-brand">
      <a href="/admin">
        <img src="/images/logo.png" height="50px" alt="FitGrocer Logo" />
        <span>FitGrocer</span>
      </a>
    </div>
    <div class="navbar-menu">
      <a href="/logout" class="logout-btn">Logout</a>
    </div>
  </nav>

  <div class="container">
    <aside class="sidebar">
      <ul>
        <li><a href="/admin">Dashboard</a></li>
        <li><a href="/admin/addProduct">Add Products</a></li>
        <li><a href="/admin/viewProduct">View Products</a></li>
        <li><a href="/admin/viewOrders">View Orders</a></li>
        <li><a href="/admin/addNutrients">Add Nutritions</a></li>
        <li><a href="/admin/viewNutritions">View Nutritions</a></li>
      </ul>
    </aside>

    <main class="main-content">
      <h1>Nutrients Listing</h1>
      <p>View Nutrients Listing</p>

      <div class="table-container">
        <table class="table">
          <thead>
            <tr>
              <th>Image</th>
              <th>Product Name</th>
              <th>Vitamins</th>
              <th>Carbohydrates</th>
              <th>Fiber</th>
              <th>Protien</th>
              <th>Fat</th>
              <th>Calories</th>
              <th>Delete</th>
            </tr>
          </thead>
          <tbody>
            <% data.forEach(element=>{ %>
              <tr>
                <td>
                  <img src="/images/<%= element.image_name %>" alt="product image" />
                </td>
                <td>
                  <%= element.name %>
                </td>
                <td>
                  <%= parseFloat(element.vitamins).toFixed(2) %>
                </td>
                <td>
                  <%= parseFloat(element.carbohydrates).toFixed(2) %>
                </td>
                <td>
                  <%= parseFloat(element.fiber).toFixed(2) %>
                </td>
                <td>
                  <%= parseFloat(element.protien).toFixed(2) %>
                </td>
                <td>
                  <%= parseFloat(element.fat).toFixed(2) %>
                </td>
                <td>
                  <%= parseFloat(element.calories).toFixed(2) %>
                </td>
                <td>
                  <a href="/admin/deleteNutrition/<%= element.product_id %>" class="btn">Delete</a>
                </td>
              </tr>
              <% }) %>
          </tbody>
        </table>
      </div>
    </main>
  </div>

  <footer class="footer">
    <p>&copy; 2025 <a href="#" target="_blank">FitGrocer.com</a></p>
  </footer>
</body>

</html>